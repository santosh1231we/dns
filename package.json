{
  "name": "dns-sinkhole",
  "version": "1.0.0",
  "description": "Custom DoH DNS server that shows a message for every site",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dns-packet": "^5.6.1"
  },
  "engines": {
    "node": ">=18"
  }
}